touch local.properties && echo googleApiKey=2 >> local.properties &&  echo keystorepath=/ >> local.properties && echo keystorePassword=dummy >> local.properties && echo gameAPIClientId=1 >> local.properties && echo firebaseProjectId=2 >> local.properties && echo keystoreAlias=3 >> local.properties && echo googleAccountServiceFile=/x.json >> local.properties && echo mixpanelToken=3 >> local.properties && echo gameAuthUrl=4 >> local.properties &&  echo firebaseToken=feo >> local.properties
